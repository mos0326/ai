# --- Web framework ---
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- Settings / validation ---
pydantic>=2.7
pydantic-settings>=2.3

# --- Database ---
SQLAlchemy>=2.0

# --- LLM / embeddings / transcription providers ---
anthropic>=0.40
openai>=1.40

# --- HTTP / scraping / math ---
httpx>=0.27
beautifulsoup4>=4.12
numpy>=1.26

# --- Tests ---
pytest>=8.0

# Optional (ローカル文字起こし。重いので既定では入れません):
#   pip install faster-whisper
# Optional (Postgres を使う場合):
#   pip install "psycopg[binary]"
